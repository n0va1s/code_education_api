{% extends 'leiaute.twig' %}
{% block conteudo %}
    <!-- Main Content -->
    <div class="container">
        <div class="row">
            <div class="col-lg-8 col-lg-offset-2 col-md-10 col-md-offset-1">
                <a href="/"><button>Novo cliente (+)</button></a>
                {% for cliente in clientes %}
                <div class="post-preview">
                    <a href="/"><h2 class="post-title">{{ cliente.nome }}</h2></a>
                    <p class="post-subtitle">O CPF é: {{ cliente.cpf }}</p>
                    <p class="post-meta">Contato <a href="mailto:{{ cliente.email }}">{{ cliente.email }}</a></p>
                </div>
                <a href="/"><button>Excluir cliente (-)</button></a>
                <hr>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}


